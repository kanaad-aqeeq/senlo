import { DEFAULT_PADDING, DEFAULT_COLOR } from "./common";

export const DEFAULT_DIVIDER_COLOR = "#cccccc";
export const DEFAULT_DIVIDER_WIDTH = 100;
export const DEFAULT_DIVIDER_ALIGN = "center" as const;
export const DEFAULT_DIVIDER_BORDER_WIDTH = 1;
export const DEFAULT_DIVIDER_BORDER_STYLE = "solid" as const;
export const DEFAULT_DIVIDER_PADDING = {
  top: 10,
  right: 0,
  bottom: 10,
  left: 0,
};




