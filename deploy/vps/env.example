# --- Security ---
# Generate a secret using: openssl rand -base64 32
AUTH_SECRET=your_auth_secret_here

# --- App Settings ---
NEXT_PUBLIC_APP_URL=http://your-vps-ip-or-domain:3000
ALLOW_REGISTRATION=false

# --- Initial Admin User (for PRIVATE mode) ---
INITIAL_USERS='[{"name": "Admin", "email": "admin@example.com", "password": "change_me_immediately", "role": "ADMIN"}]'

# --- Database (internal for docker-compose) ---
# You don't usually need to change this if using the provided docker-compose.yml
DATABASE_URL=postgresql://senlo_user:senlo_password@db:5432/senlo_db

